[project]
name = "api-gateway"
version = "0.1.0"
description = "API Gateway for multi-tenant SaaS"
authors = ["TAI Team <devs@example.com>"]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
